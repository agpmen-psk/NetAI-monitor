PySide6>=6.6
pyzabbix>=1.3.0
requests>=2.31.0
psycopg2-binary>=2.9.9
openpyxl>=3.1

# --- API-сервер и воркер (см. api_server.py / realtime_service.py) ---
fastapi>=0.110
uvicorn[standard]>=0.29
bcrypt>=4.1

# --- Десктоп-клиент (тонкий, ходит только в API) ---
keyring>=24.3
